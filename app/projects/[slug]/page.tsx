import type { Metadata } from 'next'
import { notFound } from 'next/navigation'
import { getProjects, getProject } from '../../../lib/content-repository'
import { PublicShell } from '../../components/public-shell'
export const dynamicParams = true
export async function generateStaticParams() { return (await getProjects()).map(({ slug }) => ({ slug })) }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const item = await getProject(slug); return item ? { title: item.seoTitle || item.name, description: item.seoDescription || item.summary, alternates: { canonical: `/projects/${slug}` } } : {} }
export default async function ProjectDetail({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const item = await getProject(slug); if (!item) notFound(); const jsonLd = { '@context': 'https://schema.org', '@type': 'CreativeWork', name: item.name, description: item.summary, url: `https://logikain.id/projects/${item.slug}` }; return <PublicShell><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><section className="section-pad py-20 md:py-28"><p className="mono text-[10px] text-muted">PROJECT / CASE STUDY</p><h1 className="display mt-5 max-w-4xl text-[48px] font-extrabold md:text-[82px]">{item.name}</h1><p className="mt-7 max-w-2xl text-lg leading-8 text-muted">{item.summary}</p><div className="mt-16 grid gap-10 border-t border-line pt-10 md:grid-cols-[1fr_2fr]"><p className="mono text-[10px] text-muted">THE STORY</p><div className="space-y-6 text-base leading-8 text-muted"><p>{item.body}</p><p>Case study lengkap mencakup problem, analysis, solution, implementation, technology, dan hasil yang dapat diverifikasi.</p><div className="flex flex-wrap gap-2">{item.tags.map((tag) => <span key={tag} className="border border-line px-3 py-2 text-xs text-muted">{tag}</span>)}</div></div></div></section></PublicShell> }
