'use client'
import { useState, useTransition } from 'react'
import { createRedirect } from '../../actions/admin'
export function RedirectForm() { const [pending, startTransition] = useTransition(); const [message, setMessage] = useState(''); return <form className="grid gap-3 border border-black/10 bg-white/50 p-5 sm:grid-cols-2" onSubmit={(event) => { event.preventDefault(); const form = event.currentTarget; const raw = Object.fromEntries(new FormData(form)); startTransition(async () => { try { await createRedirect(raw); setMessage('Redirect saved.'); form.reset() } catch (error) { setMessage(error instanceof Error ? error.message : 'Could not save redirect.') } }) }}><input required name="source_path" placeholder="/old-path" className="border border-black/15 bg-transparent p-3 text-xs" /><input required name="target_path" placeholder="/new-path" className="border border-black/15 bg-transparent p-3 text-xs" /><select name="status_code" defaultValue="301" className="border border-black/15 bg-transparent p-3 text-xs"><option value="301">301 Permanent</option><option value="302">302 Temporary</option></select><input name="reason" placeholder="Reason" className="border border-black/15 bg-transparent p-3 text-xs" /><button disabled={pending} className="w-fit bg-[#171717] px-4 py-3 text-xs font-bold text-[#f3f0ea]">{pending ? 'Saving...' : 'Create redirect'}</button>{message && <p className="text-xs text-[#b36f43] sm:col-span-2">{message}</p>}</form> }
