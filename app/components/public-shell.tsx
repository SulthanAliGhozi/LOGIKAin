import type { ReactNode } from 'react'
import { Nav, Wordmark } from './nav'
import { Footer } from './footer'

export function PublicShell({ children }: { children: ReactNode }) { return <><Nav /><main className="bg-[#f3f0ea]">{children}</main><Footer /></> }
export function PageIntro({ eyebrow, title, description }: { eyebrow: string; title: React.ReactNode; description: string }) { return <section className="section-pad border-b border-line py-20 md:py-28"><p className="mono text-[10px] text-muted">{eyebrow}</p><h1 className="display mt-5 max-w-4xl text-[48px] font-extrabold md:text-[82px]">{title}</h1><p className="mt-7 max-w-xl text-base leading-8 text-muted">{description}</p></section> }
export function CardGrid({ items, prefix }: { items: { slug: string; name: string; summary: string; tags?: string[] }[]; prefix: string }) { return <div className="section-pad grid gap-3 py-14 md:grid-cols-3 md:py-20">{items.map((item, i) => <a href={`${prefix}/${item.slug}`} key={item.slug} className="group flex min-h-72 flex-col bg-[#ebe4da] p-6 transition-transform hover:-translate-y-1"><div className="mono flex justify-between text-[10px] text-muted"><span>0{i + 1}</span><span className="text-lg text-orange">↗</span></div><h2 className="mt-14 text-2xl font-extrabold tracking-[-1px] group-hover:text-orange">{item.name}</h2><p className="mt-3 text-xs leading-6 text-muted">{item.summary}</p><div className="mono mt-auto pt-6 text-[9px] text-orange">{item.tags?.join(' · ')}</div></a>)}</div> }
